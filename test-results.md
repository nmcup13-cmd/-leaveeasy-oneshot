# ผลการทดสอบ LeaveEasy

**วันที่รัน:** 2026-09-18 18:30:34 – 18:30:51 (ICT)
**รันด้วย:** `npm test` (Playwright, เบราว์เซอร์ Chromium จริง) ที่ `https://leaveeasy-nan.web.app/`
**ผลรวม:** ✅ ผ่านทั้งหมด 5 / 5

---

## 1. `tests/01-submit-and-list.spec.js` — ยื่นใบลาแล้วเห็นในรายการ

**ทดสอบอะไร:** เส้นทางหลัก US-01 + US-02 — employee ยื่นใบลาใหม่ผ่านฟอร์ม แล้วตรวจว่าใบนั้นโผล่ในหน้ารายการจริง (หัวข้อ/สถานะ/ชื่อผู้ขอลาถูกต้อง)

**ผล:** ✅ PASS (7.5 วินาที)

---

## 2. `tests/02-approve-status-change.spec.js` — กดอนุมัติแล้วสถานะเปลี่ยน (รวม AI สรุปใบลา)

**ทดสอบอะไร:** เส้นทางหลัก US-04 — manager เปิดใบลาที่ยังรอพิจารณา กดปุ่ม "ให้ AI ช่วยสรุปใบลา" ตรวจว่าขึ้นป้ายข้อเสนอ AI แล้วกดอนุมัติ ตรวจว่าสถานะเปลี่ยนทันทีบนจอ + ยังคงอยู่หลัง reload (persist จริง) + ปุ่มพิจารณาหายไปหลังตัดสินใจแล้ว

**ผล:** ✅ PASS (14.2 วินาที)

---

## 3. `tests/03-empty-title-validation.spec.js` — เว้นช่องหัวข้อว่างแล้วกดบันทึก

**ทดสอบอะไร:** การตรวจสอบฟอร์ม — กรอกทุกช่องยกเว้นหัวข้อแล้วกดบันทึก ต้องไม่สร้างใบลาใหม่และต้องขึ้นข้อความเตือน จากนั้นกรอกหัวข้อกลับเข้าไปแล้วบันทึกซ้ำ เพื่อยืนยันว่าไม่ใช่บั๊กบล็อกทั้งฟอร์ม

**ผล:** ✅ PASS (9.1 วินาที)

---

## 4. `tests/04-anonymous-access-denied.spec.js` — ไม่ล็อกอินเปิดหน้ารายการ

**ทดสอบอะไร:** ความปลอดภัย US-08 — เปิดหน้ารายการใบลาโดยไม่ล็อกอิน ต้องถูกเด้งไปหน้า login เสมอ และต้องไม่มี request อ่านข้อมูล `leaveRequests` จาก Firestore เกิดขึ้นเลยก่อนถูกเด้ง (ผ่านเมื่อเข้าไม่ได้)

**ผล:** ✅ PASS (1.2 วินาที)

---

## 5. `tests/05-cross-user-access-denied.spec.js` — บัญชีที่สองเปิดใบลาของบัญชีแรก

**ทดสอบอะไร:** ความปลอดภัย US-08 — employee คนหนึ่งยื่นใบลา แล้วอีกบัญชีพยายามเปิดใบนั้นตรง ๆ ผ่าน URL ต้องเปิดไม่ได้ (ขึ้นข้อความโหลดข้อมูลไม่สำเร็จ ไม่ใช่เห็นข้อมูลจริง) (ผ่านเมื่อเข้าไม่ได้)

**ผล:** ✅ PASS (11.1 วินาที)

---

## รายการที่ไม่ผ่าน

ไม่มี — รอบนี้ผ่านครบทั้ง 5 เทสต์

## หมายเหตุ

- เทสต์ 2 จะฝากใบลาทดสอบสถานะ "อนุมัติ" ไว้ถาวรในบัญชี `leaveeasy-suite-employee2` ทุกครั้งที่รัน (ลบไม่ได้ตามกฎของระบบ — ใบที่พิจารณาแล้วลบไม่ได้ ถูกต้องตามสเปก ไม่ใช่ปัญหา)
- เทสต์อื่น (1, 3, 5) ลบข้อมูลทดสอบที่สร้างเองทิ้งอัตโนมัติหลังทดสอบเสร็จทุกครั้ง
- ต้องมีไฟล์ `.env.test` (ดู `.env.test.example`) ก่อนรันด้วย `npm test`
