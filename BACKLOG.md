# BACKLOG — LeaveEasy

สิ่งที่รู้แล้วว่ายังไม่เสร็จ หรือเป็นข้อจำกัดที่ตั้งใจปล่อยไว้ ณ ตอนส่งงาน Module 2

---

## 1. คีย์ OpenRouter ยังเปิดเผยฝั่ง client (สำคัญ)

`js/ai-config.js` (ไม่ได้ commit ขึ้น GitHub — ปลอดภัยฝั่ง repo) แต่ **บนเว็บที่ deploy จริง (Firebase Hosting) ไฟล์นี้ถูกเสิร์ฟเป็น static file สาธารณะ** ใครก็ดึงดู URL `/js/ai-config.js` แล้วเห็นคีย์จริงได้

**ทำไมยังไม่แก้ตอนนี้:** สเปก Module 2 (§0.2, §9) ห้ามเขียนเซิร์ฟเวอร์/Cloud Functions เอง — ถ้าไม่มี backend มาซ่อนคีย์ไว้ระหว่างกลาง ก็ไม่มีทางเลี่ยงการเปิดเผยจากฝั่ง client ได้เลย เป็นข้อจำกัดที่ติดมากับสถาปัตยกรรมที่สเปกกำหนด ไม่ใช่สิ่งที่แก้ได้ด้วยการเขียนโค้ดเพิ่มในขอบเขตนี้

**ทางแก้ที่ถูกต้อง (งานของ Module ถัดไป):** ย้ายการเรียก OpenRouter ไปอยู่หลัง backend เล็ก ๆ ที่ผู้ใช้แตะไม่ได้โดยตรง เช่น Cloud Functions หรือ serverless endpoint — ฝั่ง client เรียก endpoint ของเราแทน ไม่เห็นคีย์เลย

---

## 2. บัญชี hr ยังไม่ผ่านการทดสอบจริง

บัญชีทดสอบ hr (`burarak9@gmail.com`) รหัสผ่านที่มีไม่ตรงกับของจริง (ยืนยันแล้วว่าบัญชีมีอยู่จริงแต่ login ไม่ผ่าน) ทำให้ยังไม่เคยทดสอบจริงผ่านเบราว์เซอร์:
- US-06 เขียนจริง (เพิ่ม/แก้/ลบประเภทการลาโดย hr จริง — ตรวจแค่ฝั่ง Security Rules ผ่าน REST ว่าปฏิเสธ non-hr ถูกต้อง แต่ยังไม่เคยเห็น hr ทำสำเร็จจริงบนจอ)
- ส่วน hr ของ US-08 (hr เปิดใบลาได้ทุกใบเหมือน manager)
- กฎห้ามอนุมัติใบลาตัวเองสำหรับ hr โดยเฉพาะ (ทดสอบแล้วเฉพาะฝั่ง manager)

**ต้องทำ:** reset password บัญชีนี้ใน Firebase Console แล้วให้ agent `tester` ทดสอบ 3 จุดนี้เพิ่ม

---

## 3. กฎ "ต้องมีความเห็นอย่างน้อย 1 รายการก่อนกดไม่อนุมัติ" บังคับแค่ฝั่ง client

ตามหัวข้อ 6 ของสเปก — ตอนนี้บังคับด้วย JavaScript ใน `leave-request-detail.js` เท่านั้น **ยังไม่ได้บังคับใน `firestore.rules`** เพราะ Firestore Security Rules ไม่รองรับการนับจำนวนเอกสารใน subcollection ได้ตรง ๆ (ไม่มี aggregate query ในภาษา rules) หมายความว่าถ้ามีคนยิง request ตรงไปที่ Firestore API โดยข้าม UI (ไม่ผ่านหน้าเว็บ) จะกดไม่อนุมัติได้โดยไม่มีความเห็นก่อนก็ได้

**ผลกระทบ:** ต่ำ — ต้องข้าม UI ไปยิง API ตรง ๆ ถึงจะทำได้ ผู้ใช้ทั่วไปผ่านหน้าเว็บจะโดนกันไว้ปกติ แต่ยังไม่ปิดสนิทระดับฐานข้อมูล

---

## 4. คลิปสาธิต 3 นาที (สัปดาห์ที่ 9)

ยังไม่ได้ทำ — ตามตาราง §8 ของสเปกต้องมีคลิปสาธิตระบบทำงานจริงก่อนส่งมอบ

---

## 5. บัญชีทดสอบที่ค้างอยู่ใน Firebase Auth

ลบผ่าน UI ไม่ได้ (แอปไม่มีหน้า self-delete บัญชี) ต้องให้ผู้ดูแลเข้า Firebase Console → Authentication ลบเอง:
- `qa.tester.leaveeasy.20260918@example.com`
- `leaveeasy.test.accountA.20260918@example.com`
- `leaveeasy.test.accountB.20260918@example.com`

บัญชีทดสอบถาวรที่ชุดทดสอบ Playwright ใช้ซ้ำได้ (**อย่าลบ**): `leaveeasy-suite-employee1@example.com`, `leaveeasy-suite-employee2@example.com`
